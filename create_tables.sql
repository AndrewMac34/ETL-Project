CREATE TABLE fake_job_postings (
     job_id int NOT NULL
     title varchar NOT NULL
     location varchar NOT NULL
     company_profile varchar NOT NULL
     required experience varchar
     PRIMARY KEY (job_id)
);

CREATE TABLE fake_tech_postings (
     job_id int NOT NULL
     title varchar NOT NULL
     location varchar NOT NULL
     company_profile varchar NOT NULL
     required experience varchar
     PRIMARY KEY (job_id)
);


